# 程序模块与代码摘录

本文用于报告“程序模块与代码”章节。代码摘录只展示关键逻辑，不整页贴代码。

## 1. 模块说明表

| 模块 | 文件 | 核心职责 |
| --- | --- | --- |
| 课程扫描模块 | `local_course_agent/scanner.py` | 按一级文件夹识别课程，递归生成文件树 |
| 文档解析模块 | `local_course_agent/parser/`、`mineru_api.py` | PDF/Markdown/TXT/DOCX/图片解析，优先 MinerU，失败降级 |
| RAG 检索模块 | `local_course_agent/retrieval/rag.py` | 标题感知切块、BM25/RRF/vector hybrid 检索、引用来源、摘要和练习题 fallback |
| 向量检索模块 | `local_course_agent/retrieval/vector_index.py` | OpenAI-compatible embedding、本地 hash embedding、向量索引持久化和融合 |
| 大模型模块 | `local_course_agent/llm/` | 调用 OpenAI-compatible LLM 接口，支持文本、图片内容块和流式生成 |
| 文件型记忆模块 | `local_course_agent/store.py` | 保存聊天记录、课程记忆、笔记和学习计划 |
| 上传模块 | `local_course_agent/uploads.py` | 保存课程资料上传和聊天临时附件 |
| HTTP 服务模块 | `local_course_agent/server.py` | 提供 API，协调扫描、解析、检索、生成和预览 |
| 前端模块 | `frontend/src/*.vue`、`frontend/src/stores/*`、`frontend/src/services/api.ts` | Vue 三栏工作台、文件树、预览、聊天、笔记、学习计划和 API service layer |

## 2. 代码摘录一：课程目录扫描

来源：`local_course_agent/scanner.py`

```python
def scan(self) -> List[Dict]:
    if not self.root.exists() or not self.root.is_dir():
        raise ValueError(f"资料根目录不存在或不是文件夹: {self.root}")

    courses = []
    for course_dir in sorted(self.root.iterdir(), key=lambda p: p.name.lower()):
        if not course_dir.is_dir() or course_dir.name.startswith("."):
            continue
        children = self._scan_children(course_dir)
        courses.append(
            {
                "id": stable_id(str(course_dir)),
                "name": course_dir.name,
                "path": str(course_dir),
                "children": children,
                "file_count": self._count_files(children),
            }
        )
    return courses
```

说明：

- 系统把资料根目录下的一级文件夹视为课程。
- `stable_id()` 根据路径生成稳定 ID，便于前端请求文件预览和课程接口。
- `_scan_children()` 递归处理课程内部的多级文件夹。

## 3. 代码摘录二：课程内检索与引用生成

来源：`local_course_agent/retrieval/rag.py`

```python
def answer(self, course_id: str, query: str, strategy: str = "hybrid") -> Dict:
    hits = self.search(course_id, query, limit=4, strategy=strategy)
    if not hits:
        return {
            "answer": "未在当前课程资料中找到可靠依据。建议先确认该课程资料是否已完成入库，或换一种更贴近资料原文的提问方式。",
            "citations": [],
            "mode": "no_basis",
            "retrieval_quality": "none",
        }

    citations = [
        {
            "file_id": hit["file_id"],
            "file_name": hit["file_name"],
            "page": hit.get("page"),
            "chunk_index": hit["chunk_index"],
            "score": hit["score"],
            "quote": hit["text"][:180],
        }
        for hit in hits
    ]
    evidence = "\n".join(f"{idx}. {hit['text']}" for idx, hit in enumerate(hits, start=1))
    answer = (
        "基于当前课程资料，可以这样理解：\n"
        f"{_summarize_evidence(query, hits)}\n\n"
        "依据片段：\n"
        f"{evidence}"
    )
    return {"answer": answer, "citations": citations, "mode": "grounded"}
```

说明：

- 问答只在当前课程索引中检索，保证课程隔离。
- 无命中时返回“无可靠依据”，避免 AI 编造。
- 命中时生成引用来源，包括文件名、页码、片段编号和片段原文。

## 4. 代码摘录三：课程 Agent 问答

来源：`local_course_agent/server.py`

```python
def chat(self, course_id: str):
    body, uploads = self.read_maybe_multipart()
    question = body.get("question", "").strip()
    mode = body.get("mode", "answer")
    if not question and not uploads:
        return self.send_error_json("问题不能为空")
    course = CTX.find_course(course_id) or {"name": ""}
    attachment_text, image_paths = self.index_chat_uploads(course_id, uploads)
    if (attachment_text or image_paths) and not question:
        question = "请阅读并总结我拖入的文件。"
    search_question = question
    if attachment_text:
        search_question = f"{question}\n\n拖入聊天框的文件内容：\n{attachment_text[:4000]}"
    if image_paths:
        image_names = "、".join(path.name for path in image_paths)
        search_question = f"{search_question}\n\n拖入聊天框的截图：{image_names}"
    CTX.store.add_message(course_id, "user", question)
    result = CTX.kb.answer(course_id, search_question)
    answer = self.synthesize_answer(search_question, result, image_paths=image_paths)
    answer = adapt_answer_by_mode(mode, answer)
    memory = CTX.store.update_memory_from_question(course_id, question)
```

说明：

- 支持普通提问、聊天附件和截图附件。
- 附件内容只进入当前课程会话，不污染其他课程。
- 用户问题和助手回答都会保存到课程专属文件。

## 5. 代码摘录四：文件型会话记忆

来源：`local_course_agent/store.py`

```python
def add_message(self, course_id: str, role: str, content: str, citations=None, trace=None) -> None:
    messages = self.list_messages(course_id)
    messages.append(
        {
            "role": role,
            "content": content,
            "citations": citations or [],
            "trace": trace or [],
            "created_at": now_text(),
        }
    )
    self._write_json(self._messages_path(course_id), messages)

def _messages_path(self, course_id: str) -> Path:
    return self._course_dir(course_id) / "messages.json"

def _memory_path(self, course_id: str) -> Path:
    return self._course_dir(course_id) / "memory.md"

def _notes_path(self, course_id: str) -> Path:
    return self._course_dir(course_id) / "notes.json"
```

说明：

- 每门课程都有独立的 `messages.json`、`memory.md`、`notes.json`。
- 删除对应文件即可清空聊天、记忆或笔记。
- 这种方式便于同学理解和演示，不需要数据库环境。

## 6. 程序模块章节写法建议

报告中不要只贴代码，应按以下结构写：

1. 先说明模块职责。
2. 再展示 20 到 35 行核心代码。
3. 最后解释代码如何满足需求。

示例文字：

```text
课程扫描模块负责读取用户设置的资料根目录，并将一级文件夹识别为课程。该设计符合用户按课程整理资料的习惯，也避免了用户手动创建课程信息。模块会过滤不支持的文件类型，只将 PDF、Markdown、TXT、图片等学习资料加入文件树。
```
