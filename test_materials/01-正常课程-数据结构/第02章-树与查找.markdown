# 第 02 章 树与查找

二叉搜索树要求任意节点的左子树关键字小于该节点，右子树关键字大于该节点。中序遍历二叉搜索树可以得到升序序列。

AVL 树通过旋转保持平衡，任意节点左右子树高度差不超过 1。红黑树使用颜色和旋转维持近似平衡，常用于标准库映射结构。

查找边界：

- 空树查找任何关键字都失败。
- 插入重复关键字时，课程约定为更新原值，而不是新增节点。
- 删除有两个孩子的节点时，可用中序前驱或中序后继替换。

